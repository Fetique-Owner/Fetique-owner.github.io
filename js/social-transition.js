document.addEventListener('DOMContentLoaded', function() {const socialButtons = document.querySelectorAll('.social-btn');const overlay = document.getElementById('transition-overlay');const companyName = document.querySelector('.company-name');socialButtons.forEach(button => {button.addEventListener('click', function(e) {e.preventDefault();const targetUrl = this.getAttribute('data-url');overlay.classList.remove('hidden');setTimeout(() => {overlay.classList.add('active');setTimeout(() => {companyName.style.opacity = 1;setTimeout(() => {window.location.href = targetUrl;}, 2000);}, 500);}, 50);});});});